{
  "name": "@latticeag/ghostsession-worker",
  "version": "1.0.0",
  "private": true,
  "description": "GhostSession hosted relay/vault Worker — opaque signed heads, CAS, proof verification, private tunnel forward.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  }
}
